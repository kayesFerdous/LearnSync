# Quick Reference: Recurring Events Feature\n\n## Files Modified/Created\n\n### Modified Files\n1. `app/(main)/calendar/_lib/types.ts` - Added recurrence field\n2. `app/(main)/calendar/_lib/index.ts` - Exported new utilities\n3. `app/(main)/calendar/page.tsx` - Integrated recurrence UI & handlers\n\n### New Files\n1. `app/(main)/calendar/_lib/rrule-utils.ts` - RFC 5545 utilities\n2. `app/(main)/calendar/_components/recurrence-modal.tsx` - UI component\n3. `app/(main)/calendar/RECURRING_EVENTS_IMPLEMENTATION.md` - Detailed docs\n4. `frontend/IMPLEMENTATION_SUMMARY.md` - Overview\n\n## Key Functions\n\n### From `rrule-utils.ts`\n\n```typescript\n// Generate RRULE from form state\nconst rrules = generateRRules({\n  frequency: 'WEEKLY',\n  interval: 1,\n  daysOfWeek: ['MO', 'WE', 'FR'],\n  endType: 'until',\n  untilDate: new Date('2024-05-30')\n});\n// Returns: ['RRULE:FREQ=WEEKLY;INTERVAL=1;BYDAY=MO,WE,FR;UNTIL=20240530T...']\n\n// Validate recurrence strings\nif (!isValidRecurrenceArray(formData.recurrence)) {\n  console.error('Invalid recurrence');\n}\n\n// Display friendly format\nconst display = parseRRuleToHumanReadable('RRULE:FREQ=WEEKLY;BYDAY=MO,WE');\n// Returns: \"Every 1 weekly on Mon, Wed\"\n\n// UTC conversion for UNTIL\nconst utcDate = dateToUTCRRuleFormat(new Date('2024-05-30T14:40:00'));\n// Returns: \"20240530T144000Z\"\n```\n\n### In `page.tsx`\n\n```typescript\n// Handle when user applies recurrence\nconst handleRecurrenceApply = (recurrenceState: RRuleFormState) => {\n  const rrules = generateRRules(recurrenceState);\n  setFormData({\n    ...formData,\n    recurrence: rrules,\n    recurrenceState: recurrenceState,\n  });\n  setIsRecurrenceModalOpen(false);\n};\n\n// In event create/update handlers\nconst success = await calendar.createEvent({\n  // ... other fields\n  recurrence: formData.recurrence.length > 0 ? formData.recurrence : undefined,\n});\n```\n\n## Component Usage\n\n```tsx\n<RecurrenceModal\n  isOpen={isRecurrenceModalOpen}\n  onClose={() => setIsRecurrenceModalOpen(false)}\n  onApply={handleRecurrenceApply}\n  initialState={formData.recurrenceState}\n/>\n```\n\n## API Contract\n\n### Request Payload\n```json\n{\n  \"summary\": \"Math Class\",\n  \"start\": {\"dateTime\": \"2024-03-04T10:00:00Z\"},\n  \"end\": {\"dateTime\": \"2024-03-04T11:00:00Z\"},\n  \"recurrence\": [\"RRULE:FREQ=WEEKLY;BYDAY=MO,WE,FR;UNTIL=20240531T235959Z\"]\n}\n```\n\n### Response (from Backend)\n```json\n{\n  \"id\": \"event123\",\n  \"summary\": \"Math Class\",\n  \"recurrence\": [\"RRULE:FREQ=WEEKLY;BYDAY=MO,WE,FR;UNTIL=20240531T235959Z\"]\n}\n```\n\n## Recurrence Form State Structure\n\n```typescript\ninterface RRuleFormState {\n  frequency: 'DAILY' | 'WEEKLY' | 'MONTHLY' | 'YEARLY';\n  interval: number;              // 1-99\n  daysOfWeek?: string[];         // ['MO', 'WE', 'FR'] for WEEKLY\n  endType: 'until' | 'count' | 'never';\n  untilDate?: Date;              // For UNTIL\n  count?: number;                // For COUNT\n}\n```\n\n## RRULE Examples\n\n| Pattern | RRULE |\n|---------|-------|\n| Every Monday | `RRULE:FREQ=WEEKLY;BYDAY=MO` |\n| Every 2 weeks | `RRULE:FREQ=WEEKLY;INTERVAL=2` |\n| Mon/Wed/Fri until May | `RRULE:FREQ=WEEKLY;BYDAY=MO,WE,FR;UNTIL=20240531T235959Z` |\n| Daily for 10 times | `RRULE:FREQ=DAILY;COUNT=10` |\n| 1st of month forever | `RRULE:FREQ=MONTHLY` |\n| Quarterly starting | `RRULE:FREQ=YEARLY;INTERVAL=3` |\n\n## Testing Tips\n\n### Manual Testing\n1. Create event → \"Add Recurrence\"\n2. Set Weekly, Mon/Wed/Fri, until May 30\n3. Verify preview shows correct format\n4. Submit and check API request includes recurrence\n5. View event details to see human-readable format\n\n### Edge Cases\n- Event with no recurrence (recurrence array empty)\n- Editing existing recurring event\n- Changing recurrence on edit\n- Removing recurrence from event\n- UTC timezone for UNTIL dates\n\n## Common Mistakes to Avoid\n\n❌ Forgetting UTC conversion for UNTIL dates  \n✅ Use `dateToUTCRRuleFormat()` automatically\n\n❌ Missing \"RRULE:\" prefix  \n✅ `generateRRules()` always adds it\n\n❌ Sending recurrence array with empty strings  \n✅ Check `formData.recurrence.length > 0` before including\n\n❌ Not validating before API call  \n✅ Always call `isValidRecurrenceArray()` first\n\n❌ Using local timezone in UNTIL  \n✅ Always use UTC (Z-time format)\n\n## Debugging\n\n### Check generated RRULE\n```typescript\nconsole.log('Generated RRULE:', generateRRules(recurrenceState));\n```\n\n### Verify validation\n```typescript\nconsole.log('Valid?', isValidRecurrenceArray(formData.recurrence));\n```\n\n### See human-readable format\n```typescript\nconsole.log('Display:', parseRRuleToHumanReadable(rrule));\n```\n\n### Check UTC date\n```typescript\nconsole.log('UTC:', dateToUTCRRuleFormat(new Date()));\n```\n\n## Next Steps\n\n1. **Backend Integration**\n   - Ensure backend accepts recurrence in CreateEventRequest/UpdateEventRequest\n   - Validate RRULE syntax\n   - Pass to Google Calendar API\n\n2. **Testing**\n   - Unit tests for `rrule-utils.ts` functions\n   - Integration tests for create/update with recurrence\n   - E2E tests for full flow\n\n3. **Enhancements**\n   - BYHOUR, BYMINUTE (advanced constraints)\n   - Timezone support for UNTIL\n   - Recurrence rule templates\n   - Bulk import academic calendars\n\n## Support & Documentation\n\n- RFC 5545 Spec: https://tools.ietf.org/html/rfc5545\n- Google Calendar API Docs: https://developers.google.com/calendar\n- Implementation Details: See `RECURRING_EVENTS_IMPLEMENTATION.md`\n